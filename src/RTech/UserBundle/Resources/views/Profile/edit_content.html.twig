{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('rtech_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}


<div class="modal-footer">
    {#<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>#}
    {#<button type="button" class="btn btn-primary">Save changes</button>#}
    <div class="row">
        <div class="text-center">
            <button type="submit" value="{{ 'profile.edit.submit'|trans }}" class="btn btn-default fn-info-submit bouton">
                Modifier
            </button>
        </div>
    </div>
</div>

{{ form_end(form) }}
